# Sample Data

이 폴더의 `monthly_asset_returns.csv`는 baseline 계산을 검증하기 위해 만든 합성 데이터입니다.

## Columns

- `date`
- `equity_return`
- `bond_return`
- `alt_return`

## Usage

```bash
python -m src.run_baseline
```

